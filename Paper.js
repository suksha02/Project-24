class Paper{}
    var ball_options ={
        isStatic : false,
        restitution : 0.3,
        friction : 0.5,
        density : 1.2
    }

    ellipseMode(RADIUS);
    ellipse(ball.position.x,ball.position.y,18,18);

    
